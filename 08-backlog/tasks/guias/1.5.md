# Guia da task 1.5

**1.5 Create `GET /api/themes` returning themes, never cases** —
[#40](https://github.com/Concord-API/API-5/issues/40) · Backend · 6h ·
[Sprint 1](../sprint-1.md) · [todos os guias](README.md)

## O que entrega

O endpoint `GET /api/themes?q=&limit=` no formato **exato** do
[contrato da Sprint 1](../../../02-arquitetura/02-backend-dotnet.md#get-apithemes--contrato-da-sprint-1).
O frontend já está desenvolvendo contra esse JSON com `zod`: **mudar um campo exige
avisar as duas pontas**.

Critérios: a resposta é lista de temas; nenhum número de processo aparece; a chave é
`themeKey`, nunca o `theme_sk`.

## De onde vem cada campo

| Campo | Fonte |
|---|---|
| `themeKey`, `name`, `subjectArea` | `dw.dim_theme` (`theme_key`, `theme_name`, `subject_area`), pela `dw.search_themes` (1.3) |
| `judgedCount` | `dw.theme_summary.judged_case_count` |
| `outcome.upheld` / `outcome.rejected` | `dw.theme_summary.claim_upheld_count` / `claim_rejected_count` (a família de mérito; recurso **nunca** se soma a ela — [Polaridade](../../../03-dados/05-polaridade-do-resultado.md)) |
| `outcome.polarityLabel` | `dw.theme_summary.claim_polarity_label`, a frase pronta |
| `outcome.upheldRatio` | `upheld / (upheld + rejected)`, com 4 casas; **`null`** abaixo de `dw.strength_config.min_judged_for_percentage` ([D-32](../../../06-operacao/02-decisoes-e-riscos.md)) |
| `lastDecisionDate` | `dw.theme_summary.last_decision_date`, `AAAA-MM-DD` ou `null` |
| `strengthScore`, `level` | agregado de força da **2.1** (US-02) |

## O que precisa saber

**1. Depende da 1.3 e da 2.1.** `strengthScore` e `level` só existem depois da 2.1. Se a
2.1 não estiver mergeada quando você chegar aqui, fale com o Victor **antes** de mudar o
contrato; não devolva zero nem invente um valor.

**2. Siga o padrão que já existe**, o do `/health/ready`:

- uma porta em `Ratio.Application/Abstractions/` (como `ILastExtractionReader`);
- a implementação com Dapper em `Ratio.Infrastructure/Persistence/` (como
  `LastExtractionReader`), registrada em `DependencyInjection.cs`;
- o controller em `Ratio.Api/Controllers/`, com `[Route("api/themes")]`.

Identificadores em inglês (`ThemeSummary`, `ThemeSearchResult`), valores em português.

**3. Testes em duas camadas:**

- `Ratio.Api.Tests`: o `ApiFactory` troca as portas por `Mock<>`; acrescente a da busca
  e teste o formato do JSON (chaves camelCase, `themeKey`, nenhum `theme_sk`, nenhum
  número de processo);
- `Ratio.Infrastructure.Tests`: a consulta real contra o Postgres, com os `REFRESH` das
  views materializadas (ver o [README](README.md)).

**4. `limit`:** padrão 20, máximo 100. Fora disso é `400` com a mensagem do padrão de
idioma: `"O parâmetro 'limit' deve estar entre 1 e 100."`.

**5. `strength_config` precisa ter a linha.** O piso de `n` vem dela, e ela é dado da
carga, não da migration. No teste, insira
`INSERT INTO dw.strength_config (methodology_version) VALUES ('1.0')`.

**6. O que fica fora desta task:** termo curto (1.6), termo vazio (1.7), tema sem julgado
(1.8) e a ordem por força (2.3).

## Quem depende dela

1.6 e 1.7, e o frontend na hora de trocar o MSW pela API real.
